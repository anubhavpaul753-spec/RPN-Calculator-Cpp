# RPN-Calculator-Cpp
This project is a C++ implementation of a Reverse Polish Notation (RPN) calculator using the Standard Template Library (`std::stack`). Unlike standard Infix notation, RPN processes mathematical expressions by placing the operands before the operators, eliminating the need for parentheses and standard Order of Operations (PEMDAS). 
